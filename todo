x generate private/public key
x encrypt msg with public key
x decrypt msg with private key
x list nodes to talk to
x send encrypted msg to all nodes
x listen to all msgs
x decrypt msg if possible
x send msg if not possible

cannot send to self

cli to set name
cli to gen key
cli to pull and save friends key
cli to send to friend given name

cli request targets from targets

msg log file
conf to send msg to email when received
systemd for node

cap msg, send name, json size in cli and node

x node say who msg was received from
x node say who youre sending msg to

x node say time dif between when msg received and sent

add ddos'ers to config under throttle and then blocked list
